This folder contains code and files needed to create Figure X in <citation here>.

rough_CD8s.csv & rough_cells.csv are files that were created from raw histology images using the program Cell Profiler [ref].

cell_divide.R is an R script that uses base_names.txt to split the csvs by slide number and then performs classifies cells and CD8s into clusters of 100 keratinocytes each.
